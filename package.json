{
  "name": "aura-yatra-root",
  "private": true,
  "scripts": {
    "website:dev": "cd website && npm run dev",
    "backend:dev": "cd backend && node index.js",
    "app:android": "cd website && npm run build && npx cap sync android && npx cap open android"
  }
}
